// re‑export the full class (and its types)
export { ObjectId } from './objectid';
export type { ObjectIdOptions } from './types';

// default‑export the hex() helper
export { default } from './objectid';
